[profile.default]
src = "src"
out = "out"
libs = ["lib"]
auto_detect_solc = true
optimizer = true
optimizer_runs = 200
via_ir = true
# Datachain Rope node does not support Cancun opcodes (MCOPY); pin to Shanghai (PUSH0 is supported).
evm_version = "shanghai"

# Datachain Rope mainnet
[rpc_endpoints]
mainnet = "https://erpc.datachain.network"
testnet = "https://testnet.erpc.datachain.network"
localhost = "http://127.0.0.1:8545"

# Etherscan verification
[etherscan]
mainnet = { key = "${ETHERSCAN_API_KEY}", chain = 271828, url = "https://api.dcscan.io/api" }

# Testing
[profile.test]
fuzz = { runs = 1000 }
invariant = { runs = 256 }

# CI profile - faster
[profile.ci]
optimizer = true
optimizer_runs = 200
fuzz = { runs = 256 }
invariant = { runs = 128 }

# Formatting
[fmt]
bracket_spacing = true
int_types = "long"
line_length = 100
multiline_func_header = "params_first"
number_underscore = "thousands"
quote_style = "double"
tab_width = 4
