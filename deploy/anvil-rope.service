# /etc/systemd/system/anvil-rope.service
#
# DEPRECATED — superseded by reth-rope.service in production (2026-03).
# This file is kept for legacy single-host installs and for historical
# reference. New deployments MUST use reth-rope.service which provides a
# durable persisted EVM backend. See:
#   - deploy/reth-rope.service
#   - .cursor/rules/reth-blue-green-ipfs-architecture.mdc
#   - .cursor/rules/anvil-ha-architecture.mdc (SUPERSEDED notice at top)
#
# Datachain Rope EVM — Anvil (private RPC, chainId 271828)
# Binds to 127.0.0.1:8595 so only the Rope node can reach it.

[Unit]
Description=Datachain Rope EVM — Anvil (chainId 271828, private :8595)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=ubuntu
Group=ubuntu

ExecStartPre=/bin/mkdir -p /opt/datachain-rope/anvil-state

ExecStart=/home/ubuntu/.foundry/bin/anvil \
    --chain-id 271828 \
    --host 127.0.0.1 \
    --port 8595 \
    --block-time 3 \
    --gas-limit 30000000 \
    --gas-price 1000000000 \
    --accounts 1 \
    --balance 10000000000

Restart=on-failure
RestartSec=5
StartLimitIntervalSec=300
StartLimitBurst=5

StandardOutput=journal
StandardError=journal
SyslogIdentifier=anvil-rope

# Keep state file safe
UMask=0077

# Resource limits
LimitNOFILE=65536
LimitMEMLOCK=infinity

[Install]
WantedBy=multi-user.target
