# CERBER R14 - malicious IPs (auto-generated by cerber-rpc-classify.mjs).
# Do not hand-edit. Regenerated every classifier scan window.
# The default arm ("default normal;") lives in tarpit.map.conf; DO NOT
# duplicate it here or nginx will reject the map with
# "duplicate default parameter".
#
# The .include extension keeps this file out of nginx's
# `include /etc/nginx/conf.d/*.conf;` auto-glob; it is only pulled in
# from the `map { ... }` block inside tarpit.map.conf.
#
# Rows have the form:
#   1.2.3.4 malicious;  # since 2026-08-29T22:00:00Z
