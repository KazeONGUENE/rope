# Load peer-defer + loopback probe env (2026-09-01).
# Install: cp to /etc/systemd/system/erpc-fleet-ha.service.d/15-peer-defer-probe.conf
#          cp deploy/systemd/erpc-fleet-ha.env.d/peer-defer-restart.conf /etc/erpc-fleet-ha.env.d/
#          systemctl daemon-reload

[Service]
EnvironmentFile=-/etc/erpc-fleet-ha.env.d/peer-defer-restart.conf
