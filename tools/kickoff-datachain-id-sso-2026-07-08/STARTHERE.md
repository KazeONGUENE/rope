# Kickoff scaffolding for handover **datachain-id-sso** (2026-07-08)

Generated by the `handover-kickoff-dispatcher` skill at 2026-07-08T08:17:23Z.

## Next steps (in order)

1. **Author the handover .mdc** first, then update `workspaces.json` →
   `handover_reference` to point at it. The current placeholder is
   `/PATH/TO/handover-datachain-id-sso-2026-07-08.mdc`.

2. **Replace the two example `workspaces[]` entries** with the real
   projects you want to fan out across. For each:
   - `cwd`: absolute laptop path
   - `sensitivity`: `normal | health | securities | real-money`
   - `actions[]`: what this project owes from the handover
   - `emission_signals[]`: code-presence checks (ripgrep / file existence)
   - `deploy_signals[]`: live-production checks (curl / RPC / EAS)
   - `deploy_command`: pinned audited script (`null` if human-only)
   - `production_check`: `{url, expected_status, ssh_state_check?, eas_check?, rpc_check?}`
   - `min_disk_gib` / `ssh_keys_required` / `human_setup_required` (optional)
   - `primer_extra`: one sentence of project-specific context

3. **Read `~/.cursor/skills/handover-kickoff-dispatcher/LESSONS.md` first**
   before forking this — three prior rollouts hit failure modes that the
   schema-v2 contracts now prevent.

4. **`npm install`** if you want the SDK dispatcher path. The bash
   fallback (`./kickoff.sh`) needs only `jq`, `python3`, `curl`.

5. **Set `CURSOR_API_KEY`** in the env if you're using the agent path
   (any mode except `--dry-run` and `--direct-deploy`).

6. **Dry-run first**:
   ```bash
   npm run dry-run            # or
   ./kickoff.sh --dry-run
   ```
   The per-workspace summary tells you whether pre-flight passes and
   whether each project has a marker already.

7. **Plan-only on a single project** before fanning out:
   ```bash
   npm run plan-only -- --workspace=<one-project-name>
   ```
   Inspect the generated `.cursor/handover-datachain-id-sso-2026-07-08-plan.md`
   in that workspace.

8. **Emission across all projects** when you trust the plans:
   ```bash
   npm run emission           # commits + pushes; no deploy
   ```

9. **Deploy across all projects** after emission has settled:
   ```bash
   npm run deploy             # runs each project's pinned deploy_command
   ```

10. **Or full end-to-end** in one shot (less recoverable if midway things go wrong):
    ```bash
    npm run full
    ```

11. **Recovery escape hatch** — bypass the agent entirely and run the
    pinned deploy_command + production_check for one workspace:
    ```bash
    npm run direct-deploy -- --workspace=<one-project-name>
    ```

## Files

| File | Purpose |
|---|---|
| `workspaces.json` | Source of truth — replace example entries |
| `primer.template.md` | Agent prompt template (do not edit unless you understand the v2 marker schema) |
| `kickoff.ts` | SDK dispatcher (`@cursor/sdk` + `tsx`) |
| `kickoff.sh` | bash fallback (cursor-agent CLI) |
| `STARTHERE.md` | this file |
| `logs/` | auto-created on first run |

Refer to the canonical 2026-05-10 implementation for a fully-populated reference:
`/Users/kazealphonseonguene/Downloads/DATACHAIN ROPE/datachain-rope/tools/kickoff-handover-canonical-agents-2026-05-10/`
